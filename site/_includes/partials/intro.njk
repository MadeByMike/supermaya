<div class="theme">
  <header class="l-container">
    <div class="l-jumbo">
      <h1>{{ introHeading }}</h1>
      {% if introSummary %}
        <div class="l-stack">{{ introSummary | safe }}</div>
      {% endif %}
    </div>
  </header>
</div>